# 云笺集 · server 包安装配置
# 替代原 pnpm-workspace.yaml：本包为独立构建单元（§3.1），不使用 workspace 语义
minimum-release-age=0
# 与既有 node_modules/.modules.yaml 保持一致（virtualStoreDirMaxLength=60），避免 install 报错
virtual-store-dir-max-length=60
