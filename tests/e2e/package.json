{
  "name": "cloudletter-e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "云笺集 · 端到端测试（§15：⌘K、console 保存重建、可视化往返、树拖拽、版本恢复）",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@types/node": "^22.20.1"
  }
}
