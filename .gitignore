# ============================================================
# 云笺集 · CLOUDLETTER — .gitignore
# 分区：测试 / 文档 / 依赖 / 构建产物 / 运行时数据 / 环境变量 / 测试产物 / 本地工具状态 / 编辑器与系统 / 防误提交
# 仓库边界约定（2026-09-09 起反转：仓库只保留源代码与工程配置）：
#   - tests/                = 本地测试（单测 + e2e），仅保留在本地工作区，不入库
#   - docs/                 = 设计规范 + 部署文档 + 上下文记忆，仅本地
#   - README.md / AGENTS.md = 项目文档，仅本地
#   - 源代码 apps/ server/ 与工程配置（package.json / tsconfig / eslint / prisma / deploy）正常入库
# ============================================================

# --- 测试（单测 + e2e 全部本地；本地运行：pnpm -C server test / pnpm -C apps/workbench test / pnpm -C tests/e2e test）---
tests/
test-results/

# --- 文档（仅本地）---
docs/
README.md
AGENTS.md

# --- 依赖 ---
node_modules/

# --- 构建产物与编译缓存（源码可再生）---
dist/
tsconfig.tsbuildinfo
*.tsbuildinfo
**/node-compile-cache/
**/tsx-*/

# --- 运行时数据：SQLite 数据库与 WAL 日志 ---
*.db
*.db-journal
*.db-wal
*.db-shm

# --- 部署包与本地日志 ---
*.tar.gz
*.log
.dev-*.err

# --- 环境变量（含密钥，禁止入库；.env.example 模板正常提交）---
.env
.env.production

# --- 本地工具状态（AI 助手记忆 / 本地缓存，非源码）---
.codebuddy/
.workbuddy/
.zcode/
.playwright-cli/

# --- 编辑器 / 操作系统 ---
.idea
*.iml
.DS_Store

# --- 防误提交：非 pnpm 工具链的锁文件（本项目统一用 pnpm，见各包 pnpm-lock.yaml）---
package-lock.json
yarn.lock
bun.lockb
