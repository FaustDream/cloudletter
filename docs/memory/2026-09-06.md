# 2026-09-06

## 导航重构 + 日常×计划联动 + 游戏开关（2026-09-06（二））

### 用户决策（grill 确认）

- 专注历史存储：**新增专注记录表**（否决复用灵感笔记方案）——番茄选定计划专注后落记录，时间轴出「专注」节点，日常页看历史。
- 其余按用户明示执行：讨伐/经验升级默认关闭收进设置「游戏」子菜单；目标三件套升为侧边栏独立菜单；计划加 P4·不紧急；速记弹窗居中+可拖拽。

### 改动内容

- **路由/导航**：新增 /goals /plan /checkin 独立路由（GoalsPage/PlanPage/CheckinPage withHeader 独立渲染，页头改名 目标/计划/习惯）；/goals-home 聚合页保留兼容；侧栏 NAV_ITEMS 改为 云笺/灵感/文章/日常/计划/目标/习惯/检索/记账本/分类标签/设置，「灵感笔记」→「灵感」。
- **P4 档位**：schema PlanItem.level 注释、workbench WB_SCHEMA oneOf、game.ts PlanLevel/PLAN_RANGES（P4: xp 3-6, gold 0-1）、前端 PlanItem 类型/LEVELS/三处下拉/分组顺序。
- **FocusLog**（schema 新表 focusLog：planId/planTitle 快照/minutes/date + date/planId 索引；纯新增表，db push 无损）：workbench 新 scope `focus`（MODEL/FIELDS/WB_SCHEMA minutes min1 max600/requiredAtCreate/orderBy date）；timelineData 增 focusLogs 取数 + 「🍅 专注 N 分钟」节点（t='focus', xp=0 不涉游戏化）；dashboard totals 增 focus 计数。
- **时间轴 focus 类型全接入**：api.ts 与 timeline.ts 的 TimelineType 并集、TL_TYPES/TL_COLOR(#475569)/TL_DESC、TimelineBubbles TYPE_SIDE(L)/TYPE_EMOJI(🍅)、TimelineCity ZoneData、TimelineUniverse3d TYPE_EMOJI、DataCore statOf；DashboardStats totals 类型随 api 并集自动补齐。
- **日常页联动 UI**：番茄卡内 Dropdown 选计划（🆓 自由专注/📋/✅ 前缀区分完成态）；完成后选了计划→POST /workbench/focus，未选→维持写灵感笔记（标签=习惯）；今日番茄/分钟/本周统计合并 focusLogs；新增「专注执行历史 · 今日」列表（时间/分钟/计划名）。
- **编辑器消失再修（服务器端路径）**：① 初版解析完成前 `BlockNoteView editable={ready}`——打字/选词不再落空文档被 replace 冲掉；② 外部值回灌 `replaceFromMarkdown(value, true)` 解析失败保持现有内容并同步 lastEmitted（防无限重解析）。
- **速记弹窗**：`.nmodal` margin:auto 安全居中 + max-height calc(100dvh-48px) 内滚；标题栏按住拖拽（QN_POS_KEY 位置记忆、双击复位居中、clamp 防拖出屏幕）。
- **游戏开关**：`lib/gamePrefs.ts`（cl_game_prefs，默认 battle=false/xp=false）；设置页新增「游戏」分组（讨伐模式/经验与等级 两开关，seg 启停，本机偏好立即生效）；OverviewPage 按开关隐藏 讨伐卡/升级庆祝/头像 LV 徽标，3D 宇宙 ⚔️ 入口传 battle prop 显隐；速记 +XP 文案随开关。

### 验证与部署

- 双端 typecheck 干净；服务端单测 125/125、前端 98/98；vite build 通过。
- 时间轴 focus 类型波及面：TNodeType（server）、api.ts/timeline.ts 双 TimelineType、TimelineBubbles/City/Universe3d/DataCore 各映射表逐一补齐后 typecheck 才绿——新增节点类型务必 grep `Record<TimelineType` 全量补。
- 部署：备份 `2026-09-06T04-35-54` → DEPLOY.md §3（清 dist + env -u NODE_ENV）→ db push 建 focusLog 表（纯新增无损）→ 重启探活 ok。
- 验证：公网哈希 index-CdlLa0el.js = 本地 dist；/plan /goals /checkin 均 200；/api/v2/workbench/focus 返回鉴权包络（路由存活）；focusLog 表经 prisma count() 访问正常（0 行）。

### 备注

- 服务器时间已到 2026-09-06，备份名/本条 memory 均按当日日期。
- 编辑器"内容消失"两条服务器端路径（初版解析窗口打字、外部值解析失败）已封；若用户仍复现，需再采集具体操作序列。
- 用户本地 3011 dev server 常驻运行，复现类工作用 3013+3016 端口组合起 e2e 库环境，避免端口冲突。
