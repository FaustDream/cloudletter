# 2026-09-05

## 测试文件统一迁移至 tests/ 目录

### 改动内容

将散落在源码目录中的 23 个单测文件统一迁移至仓库根 `tests/` 目录，按「被测源码所在文件夹」分组镜像：

- `server/src/*.test.ts`（auth / content / crypto / game / util-date）→ `tests/server/`
- `server/src/routes/*.test.ts`（8 个路由单测）→ `tests/server/routes/`
- `server/src/middleware/validate.test.ts` → `tests/server/middleware/`
- `apps/workbench/src/lib/*.test.ts`（date / exporters / markdown）→ `tests/workbench/lib/`
- `apps/workbench/src/components/editor/*.test.ts`（customBlocks / outline / revision）→ `tests/workbench/components/editor/`
- `apps/workbench/src/components/timeline/*.test.ts`（range-picker / timeline）→ `tests/workbench/components/timeline/`
- `apps/workbench/src/pages/search-highlight.test.ts` → `tests/workbench/pages/`

全部使用 `git mv` 迁移，保留文件历史。

### 配套修改

- 测试文件内相对导入（含 `vi.mock`、动态 `import()`、`typeof import()` 的模块说明符）统一改为 `@server/*`、`@workbench/*` 别名，别名在 `server/vitest.config.ts`、`apps/workbench/vitest.config.ts`（`resolve.alias`）与两包 `tsconfig.json`（`paths`）中同步维护。
- 两包 vitest `include` 改为指向 `tests/` 下对应分包（`../tests/server/**`、`../../tests/workbench/**`）。
- 两包 `tsconfig.json` `include` 扩展到对应 tests 分包；因测试文件在包外、无法沿 node_modules 解析，`paths` 中显式映射 `vitest`（server 另映射 `express` 类型）。
- `server/package.json` lint 脚本扩展为 `eslint . ../tests/server`（eslint 当前未安装，脚本暂不可运行，为既有状况）。
- `docs/DESIGN-SPEC.md` 同步更新单测文件约定（§1.1 命名表、§2.1 顶层结构、§2.3 lib 注释）。
- 新增根 `README.md`（此前不存在）：项目简介、目录结构、单测布局约定、常用命令与本次变更记录。

### 验证

- 后端单测：14 文件 / 120 用例全部通过
- 前端单测：9 文件 / 50 用例全部通过
- 两包 `tsc --noEmit` 类型检查无回归
- 本地开发服务器正常启动：server :3011（/healthz 200）、workbench :3015

### 备注

- `e2e/` 为独立 Playwright 工作区（自带 package.json / playwright.config.ts），不属于「散乱测试文件」，保持原位未动。
- 注意：当前工作区 `.gitignore` 有未提交改动将 `tests/` 加入忽略列表；被 git 追踪的测试文件不受影响，但后续在 `tests/` 下新增测试文件时 `git add` 会被静默跳过，需移除该行以保持单测入库。

---

# 2026-09-05（二）：仓库边界重划——tests/ 转本地目录，docs/ 回归版本控制

## 改动内容

- 全面复查确认：旧的散乱测试文件已无残留（`*.test.*` / `*.spec.*` / `__tests__` 等模式全仓扫描，仅剩 `server/src/test-utils.ts` 为测试辅助工具而非测试文件，保留在源码中）。
- `tests/` 转为本地目录：上一轮已提交的 23 个测试文件自版本控制移除（`git rm -r --cached`，磁盘文件保留），`.gitignore` 忽略 `tests/`。本地 vitest 运行不受影响。
- `docs/` 取消忽略，重新纳入版本控制（DESIGN-SPEC.md + memory/）。
- `.gitignore` 重写整理：分区清晰（依赖 / 构建产物 / 运行时数据 / 环境变量 / 测试产物 / 本地工具状态 / 编辑器与系统 / 防误提交），文件头注明仓库边界约定；移除过时条目（`.astro/`、`.vercel`），散列的 debug 日志条目合并为 `*.log`，保留非 pnpm 锁文件防误提交。
- README.md 同步更新（结构标注 tests/ 不入库、变更记录追加、单测数量 25→23 更正——server 14 + workbench 9，此前误记 25）。

## 验证

- 磁盘上 tests/ 文件完整保留，`pnpm -C server test` / `pnpm -C apps/workbench test` 可正常运行（170 用例）。
- 提交后 `git status` 干净（仅 AGENTS.md 保持用户未提交编辑）。

## 备注

- 历史 commit 3509cc8 中仍含 tests/ 文件（正常历史，未改写）；如需从历史彻底清除需改写历史，未执行。
- AGENTS.md「测试要求」仍写"测试文件放在 tests 目录"——物理布局约定不变，tests/ 仅不再随仓库分发；该文件保持用户未提交编辑，未代为修改。
