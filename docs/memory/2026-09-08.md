# 2026-09-08 改动记录

## 本地默认管理员口令改为 123456

**需求**：修改本地项目默认密码为 123456。

**改动**：

- `server/src/seed.ts`：口令策略调整
  - 本地（非生产，`NODE_ENV !== 'production'`）：未设置 `ADMIN_PASSWORD` 时默认口令 `123456`（长度放宽至 ≥6 位，绕过弱口令黑名单）。
  - 已有管理员：口令不一致（`verifyPassword` 校验失败）时自动更新为当前口令（等价于重置管理员口令）；一致则幂等跳过，重复执行不重复重置。
  - 生产（`NODE_ENV === 'production'`）：维持原 🔴K2 约束——必须显式提供 ≥8 位且不属于弱口令黑名单（含 123456），缺失/过弱拒绝创建，禁止默认凭据上线。
- `server/.env.example`：`ADMIN_PASSWORD` 注释更新（本地默认 123456、seed 可重置、生产强口令要求），示例值保持注释掉。
- `docs/DESIGN-SPEC.md`：§2.2 seed 说明、§7 认证条目同步。

**执行与验证**：

- `pnpm -C server seed`（corepack 调用）→ `管理员口令已重置为 默认口令 123456: 935411629@qq.com`。
- 二次执行 seed → `管理员已存在且口令一致`（幂等）。
- 临时脚本校验 `verifyPassword('123456', hash) === true`（已删除该脚本）。
- 后端 `typecheck` 通过。

**生效范围**：仅本地 dev 库（`server/data/cloudletter-dev.db`，管理员 `935411629@qq.com` 现口令 123456）。生产凭据策略未变。