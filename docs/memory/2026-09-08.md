# 2026-09-08：邮件链接 origin 修复（已上站）

## 用户报障

忘记密码邮件链接为 `http://localhost:3011/login?reset=...`（后端端口），打开显示 `{"error":{"code":"NOT_FOUND","message":"接口不存在"}}`；并问生产环境是否一样。

## 根因

- `auth.ts send-reset` / `grants.ts POST /` / `rss.ts GET /rss.xml` 三处的 origin 都是 `${req.protocol}://${req.get('host')}`——取 **API 请求的 Host 头**。
- 本地：vite 代理 `changeOrigin: true` 把 Host 改写为后端端口（localhost:3011）→ 链接指向后端；`/login` 是前端 SPA 路由 → Express 兜底返回应用级 404「接口不存在」。
- 生产不一样：nginx `location /api/` 配了 `proxy_set_header Host $host`，后端拿到的 Host = 公网访问地址（121.37.222.241），链接 `http://121.37.222.241/login?...` 经 `location /` SPA 兜底正常打开——**生产一直正常，无需处理历史邮件以外的东西**。

## 修复

- 新增 `server/src/lib/webOrigin.ts`：`WEB_ORIGIN` env（服务器显式配置，最高优先，尾斜杠归一）> 请求 `Origin` 头（浏览器 fetch 天然携带 = 真实前端站点；畸形值/"null"/带路径忽略）> API Host 兜底（与原行为一致）。
- 三处统一接入 `webOrigin(req)`；服务器可选在 `.env.production` 加 `WEB_ORIGIN` 加固（不配也正确）。

## 验证与部署

- typecheck 干净；server 单测 135/135（新增 `tests/server/lib/webOrigin.test.ts` 6 例）。
- 本地 dist 发现为 09-07 15:16 旧构建（index-z9hfL0KQ.js，非 HEAD 产物），弃用并从 HEAD 重建 → `index-DABrgZXx.js` 与线上完全一致（前端无改动，确定性构建复现同哈希）。
- commit 1d717f0（webOrigin.ts + auth/grants/rss 接入）。
- 部署：备份 `2026-09-08T01-38-08` → install → 停服 drop-fts + db push（no-op）→ 前端就位 → 重启探活 → nginx reload。
- 公网验证：入口哈希不变 `index-DABrgZXx.js`；`/` `/posts` 200、`/api` 401 鉴权包络；实测带 `Origin` 请求 `/api/v2/rss.xml`，feed 内 `<link>` 正确随 Origin 生成——新逻辑上站生效。
- 本地 dev（tsx watch）已热重载：登录页重新发起忘记密码，链接应为 `http://localhost:3015/login?reset=...`（浏览器 Origin=3015）。

# 2026-09-08（二）：SMTP 发送失败从 500 改为明确 503 + 弹窗去 ×（已上站）

## 用户报障

1. 生产打开重置密码弹窗，发送重置链接「无反应」：POST /auth/send-reset 500 INTERNAL，弹窗未关。
2. 重置密码弹窗不要右上角 × 关闭按钮。

## 根因（日志确诊）

- `journalctl`：`Invalid login: 535 Login fail...`（QQ 邮箱 SMTP）——**SMTP 账号登录失败**（授权码失效/账号异常/限频/服务未开），与 origin 修复无关（部署前 09:23 即在报错）。
- 代码放大了糟糕体验：`mailer.ts sendMail` 的 catch **脱敏后故意抛出**（注释约定 auth 经 ah() 包络为 500）；而全部 6 个调用点其实都已有 `if (!mail.sent) return 503` 分支——抛出使这些分支永不生效，前端只见笼统 500。
- 「弹窗未关闭/无反应」：sendReset 失败时 `setErr` 写入的是登录卡片里的 `lerr`，被 Modal 遮住，用户什么都看不到。

## 修复（commit 272bc41）

- `mailer.ts`：catch 改为 `return { sent:false, reason: 'Name: message'（截断 300） }`——所有调用点的 503 分支立即生效。
- `Modal.tsx`：新增 `hideClose` prop（隐藏 ×；Esc/点遮罩/页脚按钮关闭途径保留）。
- `LoginPage`：忘记密码弹窗传 `hideClose`；弹窗内新增就地 `lerr` 错误显示。
- 新增 `tests/server/mailer.test.ts`（2 例）：SMTP 指向 127.0.0.1:1（ECONNREFUSED 毫秒级、无外网依赖）断言失败不抛出且不泄漏凭据。注：vi.mock('nodemailer') 在包外测试文件中未拦截 SUT 的动态 import（真实连接致超时），故改用本机不可达端点方案。

## 验证与部署

- server 137/137、workbench typecheck 干净、build 通过（index-DBd_3fZU.js）。
- 部署：备份 `2026-09-08T02-17-11` → 前端就位 → 重启探活。
- 生产实测：`POST /auth/send-reset`（合法 JSON）→ **503 SMTP_DISABLED** + 535 原因明文返回（此前 500）；首次用 curl 验证时 JSON 被 PowerShell 引号转义弄坏产生过一次 JSON parse 500（body-parser 层，非业务），改用 `--data @file` 复测。
- **待用户处理**：QQ 邮箱 SMTP 535 = 服务器 `.env.production` 的 SMTP 凭据/账号问题（授权码失效/改密/IMAP/SMTP 服务未开/发送限频），需登录 QQ 邮箱检查授权码与账号状态；代码只能把失败讲清楚，发信能力本身取决于账号。

## 踩坑记录

- PowerShell 传 curl 的 JSON 体：`\"` 转义会丢引号 → body-parser 500；带 JSON 的请求一律 `--data @file`。
- 包外测试文件 vi.mock 不拦截包内 SUT 的动态 import（ vitest 2.1.9 实测），网络类依赖改用「本机不可达端点」测失败路径。

# 2026-09-08（三）：生产 SMTP 535 真因确诊并修复——授权码与本地不一致

## 用户疑问

本地都能发送邮件，为什么生产 535 失败？

## 对比诊断（服务器上 A/B 实测）

- 本地 `.env`：`SMTP_HOST=120.232.69.34`（钉 QQ SMTP 真实 IP，注释说明本机代理 fake-IP 劫持 smtp.qq.com 的 DNS，故 servername 固定域名）+ 有效授权码 → 本地发送一直成功。
- 生产 `.env.production`：`SMTP_HOST=smtp.qq.com` 域名直连，TLS 握手正常，AUTH 535；自 09-07 以来 send-reset 4 次全部失败、从未成功。
- **决定性 A/B**：同一台生产服务器、同一出口 IP，`node smtp-test.cjs <pass>`（nodemailer verify）分别测两组授权码——本地码 → **AUTH OK**；生产存的码 → **535 FAIL**。排除 IP 风控/限频/服务未开，实锤 = **生产 .env.production 的 SMTP_PASS 是旧/错误授权码**（错误信息里 Account abnormal / service not open / frequency limited 均为 QQ 笼统话术）。

## 修复与验证

- 服务器 `.env.production` 备份为 `.env.production.bak-20260908` → `SMTP_PASS` 同步为本地同款 → chmod 600 → 重启服务。
- 实测 `POST /auth/send-reset` → `{"ok":true,"sent":true,"mode":"smtp"}`，真实邮件送达（发件=收件=管理员邮箱，白名单内）。
- 提醒：两端 SMTP_PASS 今后保持一致；boot 日志「SMTP 已就绪（TLS 校验通过）」只是配置闸门检查，不代表 AUTH 可用，不能作为发信能力的证据。

# 2026-09-08（四）：密码重置流程安全完善（已上站）

## 需求清单与现状盘点

用户列 6 项要求，逐项盘点后发现 4 项已具备、2 项为本轮工作：

| 要求 | 现状 |
|---|---|
| 邮件链接形式 + 安全校验令牌 | 已有：64 位 hex 密文仅出现在邮件链接，库内只存哈希（sha256） |
| 链接有效时限 | 已有：15 分钟（RESET_TTL_MS），过期/已用一律 401 |
| 防链接泄露 | 已有：令牌一次性（消费即 usedAt）、比对失败一次即作废（防在线穷举）、错误不区分邮箱是否存在（防枚举） |
| 重置成功后原密码立即失效 | 已有：passwordHash 更新 + 全部会话撤销（含发起重置的浏览器） |
| 已登录点击链接跳首页（bug） | **本轮修复** |
| 新密码强度校验 | **本轮补齐** |

## 改动（commit 1e08031）

- **App.tsx `LoginRoute`**：已登录一律跳首页 → 带 `?reset=` 参数时放行直达「设置新密码」面板（重置成功本就撤销全部会话，无安全副作用）。
- **auth.ts `/auth/reset`**：接入 `passwordRule.passwordPolicyError`（必须同时包含中文与英文字母），与长度护栏共同生效；**校验先于 token 消费**——弱密码 422 后同一 token 仍可换合规密码重试（测试锁定该行为）。
- **auth.ts `/auth/password`**（改密码）：同步接入复杂度策略——设置页 UI 早已宣示「必须同时包含中文与英文字母」但服务端未执行，本次补齐（全站密码强度口径一致）。
- **LoginPage**：重置表单客户端预校验复杂度；面板提示补「8-128 位，须同时包含中文与英文字母；链接仅一次性有效（15 分钟内）」；重置成功后 `setToken(null)` 清理本地已撤销会话的失效凭据。
- 测试：auth-routes.test.ts 适配 + 新增（改密码弱密码 422；重置弱密码 422 且 token 不消费、换合规密码可重试）。

## 验证与部署

- server 138/138（新增 2 例）、双端 typecheck 干净、build 通过（index-BUwWBxOH.js）。
- 部署：备份 `2026-09-08T02-57-41` → 前端就位 → 重启探活。
- 生产实测（用无效令牌，不触碰真实重置链路）：弱密码 → 422「密码必须包含至少一个中文字符」（策略生效且先于令牌校验）；合规密码 + 无效令牌 → 401「重置链接无效或已过期」。

## 踩坑记录

- 测试密码「重置pw123」长度仅 7 位，先被「至少 8 位」拦截——组合规则测试要先过长度关。

# 2026-09-08（五）：密码规则放宽为数字+英文 + 个人中心菜单透明度根治（已上站）

## 用户两项

1. 密码强度校验放宽：只要有数字和英文即可（废止「必须含中文」）。
2. 个人中心菜单（头像下拉）半透明，页面文字透出。

## 根因与修复

### ① 密码规则（commit 14bb86b）

- `lib/passwordRule.ts`：`passwordPolicyError` 从「中文+英文」改为「**数字+英文**」（纯数字提示缺字母、纯英文提示缺数字；中文/符号可选）。此前该库是孤儿（服务端零引用），昨日接进 reset/改密码两条路由后规则口径随本轮一并更新。
- 前端同步：LoginPage 重置预校验与面板提示、SettingsPage 改密码校验与两处文案。
- 新增 `tests/server/lib/passwordRule.test.ts` 4 例；auth-routes 弱密码用例改为纯英文无数字（'weakpassword'/'plainpassword'，旧用例密码含数字在新规则下已合规）。

### ② 个人中心菜单半透明（同一 commit）

- 根因：`.sidebar .foot { opacity: .7 }`——菜单从侧栏底部头像弹出、DOM 属于 foot 子树，**祖先级 opacity 作用整棵子树**（子元素 opacity:1 无法逃逸，L343 的旧补救无效），菜单整体 70% 不透明 → 页面文字透出。
- 修复：删除 foot 的 opacity，弱化语义改由 `color: var(--drawer-text-mute)` 表达；`.am-menu` 的实底白/暗色规则（e998bc6）保持不变。经验：**浮层类组件的祖先链路上禁用 opacity/filter**（会传染整棵子树且子元素无法逃逸）。

## 验证与部署

- server 142/142（passwordRule 4 例新增）、双端 typecheck 干净、build 通过（index-aD80FQp-.js）。
- 部署：备份 `2026-09-08T03-14-19` → 前端就位 → 重启探活。
- 生产实测：纯英文无数字 reset → 422「密码必须包含至少一个数字」；数字+英文 → 401 令牌无效（策略通过）；菜单透明度待用户浏览器确认（强刷后）。

---

## 总览四世界：导航样式修复 + 时光长河日期筛选整合（未上站）

### 用户报障

1. 云笺（总览）上「时光 / 神经 / 城市 / 核心」四个分类的样式失效。
2. 时光长河上下滚动会按顶部日期档位（当天/近 7 天等）自动切换；要求：滚轮只翻页，去掉顶栏的「当天/7天」档位按钮组，只保留一个日期控件，并把快捷筛选收进日期控件；日期控件图标换用用户提供的日历 SVG（原为 🗓 emoji/非矢量）。

### 修复

- **样式失效双根因**：
  1. `main.css` `.world-nav` 规则括号错位：`position: relative; z-index: 3; }` 提前闭合，后续 `background/border/border-radius/box-shadow/backdrop-filter/max-width/flex-wrap` 全部成为规则外孤儿声明（CSS 静默丢弃），导航条玻璃背景/阴影/圆角丢失。已修复为标准单规则（配平校验：全文件 0 孤儿、depth 0）。
  2. 「去容器化·轻度融合」在 `.main` 作用域把 `--surface-card`/`--shadow-card` 覆写为 `transparent`/`none`，`.world-nav` 引用这两个变量 → 即使语法修复后背景仍透明。导航条是悬浮控件，已改为一律使用 `--surface-nested` 82% 玻璃背景 + 固定 `0 8px 24px` 阴影 + `backdrop-filter: blur(12px)`（注释说明原因）。
- **时光长河日期整合**：
  - `RangePicker.tsx`：快捷档位改为 当天 / 近 7 天 / 近 30 天 / 本月 / 近一年（动态计算，`quickStateOf(v)` 由档位值构造完整 RangeState；原 60 天档位移除）；trigger 图标 🗓 emoji → 内联日历 SVG（用户提供，7 个 path，fill #03A9F4，类 `.rp-cal-ic` 垂直对齐）；原 `quickToRange` 删除。
  - `OverviewPage.tsx`：删除 `SCALES`/`scaleIdOf`/`scaleId`/`onRiverWheel` 与顶栏 `.scale-seg` 按钮组；`.river-scroll` 移除 `onWheel` 拦截，滚轮恢复自然翻页；空态与副标题文案改为提示用右上角日期控件。
  - `main.css`：删除无人使用的 `.scale-seg/.scale-btn` 规则，新增 `.rp-cal-ic` 样式。
- 单测更新：`tests/workbench/components/timeline/range-picker.test.ts` 改用 `quickStateOf`，新增 当天/本月/未知档位回退/新文案 4 例。

### 验证

- 前端 typecheck 通过；前端全量单测 129/129。
- 真实浏览器验证：4 个分类导航条背景/阴影/圆角/blur 恢复（computed：`color(srgb .91 .95 1/.70)` + `0px 8px 24px` + 14px + blur 12px）；城市/核心/时光视图切换正常；日期控件 SVG 图标（#03A9F4）与快捷档位文字齐全；河流视图滚轮多次滚动，日期控件文字保持「近 30 天」不自动切换。

### 部署（已上站）

- 工作区存在未提交在途改动（Modal hideClose / LoginPage 重置弹窗错误显示 / mailer 503 reason 及配套测试，测试 2 例超时因 vi.mock 对动态 import 失效连真实 SMTP），为不带上线：按 DEPLOY §7 先例 `git worktree add <dir> HEAD` 干净检出出包（vite 构建用 `node node_modules/vite/bin/vite.js build`，node_modules 用 Junction 联接；pnpm 在 worktree 内无法运行）。
- 产物：`index-DumZT5GR.js` + `index-hTVsjxQB.css`（与主工作区含 WIP 的 `index-DBd_3fZU.js` 区分）。tarball 186 项，scp → 远端脚本（bash -s + stdin 管道，避免 PowerShell 引号转义问题）。
- 服务器侧：backup → 解包 → `unset NODE_ENV && pnpm install` → 停服 drop-fts + db push（no-op）→ 起服 → workbench 就位 → 探活 ok → nginx reload。
- 公网验证：服务 active、healthz `{"ok":true}`、`/` 入口 `index-DumZT5GR.js` + `index-hTVsjxQB.css` 与本地 clean 产物一致、`/posts` `/overview` 200；/api 鉴权包络正常。
- 备注：DEPLOY §7 已知「解包叠加旧产物」坑再次出现（workbench/assets 残留旧 `index-DABrgZXx.js`，入口 HTML 只引用新哈希、无实际影响），下次部署再彻底清理。

---

## 总览：移除世界导航提示文案（已上站）

用户要求去掉 `.world-nav-tip`（「同一份数据 · 四种解释方式」）。移除 OverviewPage 中该 span 及 main.css 中 `.world-nav-tip` 规则与媒体查询隐藏规则。

- typecheck 通过；commit 6f5abff。
- 部署：worktree 干净出包（HEAD 6f5abff），入口 `index-CUd99g1q.js` + `index-DEBNaqu3.css` 与本地一致。
- 改进部署脚本：`rm -rf` 旧 dist/workbench 提前到 `tar` 解包**之前**执行，本次线上 `workbench/assets` 仅含新产物（旧 `index-DABrgZXx.js` 残留被清掉）——DEPLOY §7 的叠加残留坑从本轮起在脚本层面规避。
- 公网验证：healthz ok、`/` 入口哈希一致、服务 active。

---

## 总览三项体验调整（已上站）

1. **类型筛选改多选下拉**：新增 components/timeline/TypeFilter.tsx（全部/7 类型勾选，多选为「或」）；filterNodes 签名改 TimelineType[]（空=全部）；TimelineBubbles/Universe3d/City/DataCore 四视图同步；OverviewPage filter 状态改数组。单测更新 3 例（131/131）。
2. **速记 FAB 折叠**：46px 圆形纯 ⚡ 贴左下边（18,18），无文字；拖拽边界按 64px 收紧；双击复位保留。
3. **个人中心菜单实底增强**：.am-menu 保持 --surface-solid 不透明 + 双层柔和阴影（0 18px 44px rgba(15,30,60,.3) + 0 4px 14px），浏览器实测 background rgb(255,255,255) 不透明。

验证：typecheck 干净、单测 131/131、真实浏览器（多选筛选生效/纯图标 FAB/menu 实底）。commit 0f99817。
部署：worktree 干净出包，线上入口 index-zo7eYvP6.js + index-Xdev7xJn.css 与本地一致，healthz ok、assets 无旧残留。

---

## 个人中心菜单实底再增强（已上站，commit e998bc6）

用户反馈菜单"透明底太高、内容与背景融为一体"。本地诊断：light 下 .am-menu 实为不透明白底，深色主题下 --surface-solid=#141d2e 与页面 #0d1420/侧栏深蓝过近是主因（融为一体）。
修复：.am-menu 锁定深浅两套高对比实底（不随主题 --surface-solid 覆写）：
- 浅色：#ffffff + rgba(120,160,220,.4) 描边 + 双层阴影；
- 深色：#1b2742（比页面明显更亮一档）+ rgba(130,170,235,.32) 亮描边 + 深阴影。
- .am-st 状态格改用 --accent-muted 底（原 --surface-solid 与实底融合）。
双主题浏览器复验：light 白底黑字、dark #1b2742+亮描边均从侧栏清晰浮出，文字可读。typecheck 通过。
部署：worktree 干净出包，线上入口 index-CqdV_394.js + index-CACKnfUv.css 与本地一致，healthz ok。
