# 2026-09-07

## 灵感笔记增加使用状态（待使用/已使用/已过期）

### 需求背景

用户反馈：灵感笔记一直堆积，不知道每条处于什么状态（是否已使用/已过期），需要一个显式的状态字段来标记。

### 决策

- 状态机只保留用户可控的三种状态：`pending`（待使用，新建默认）/ `used`（已使用）/ `expired`（已过期）；不引入自动过期逻辑（不依赖日期自动翻转），由用户手动标记，语义干净。
- 状态字段放在 `NoteItem.status`（String + default 'pending'），**纯新增列、带默认值，`prisma db push` 非破坏性**，走 DEPLOY.md §3 标准流程即可，无需中间态/迁移脚本三段法。

### 改动内容

- **schema**：`server/prisma/schema.prisma` NoteItem 增加 `status String @default("pending")` + 注释。
- **后端** `server/src/routes/workbench.ts`：
  - `WB_SCHEMA.notes` 增加 `status: oneOf ['pending','used','expired']`（非法值 422）；
  - `FIELDS.notes` 白名单加入 `status`；
  - `createNote` 缺省 pending、`updateNote` 支持改 status；
  - `NoteWithTax` 增加 status，`shapeNote` 透出。
  - 时间轴灵感节点：非「待使用」的笔记预览加前缀（`已使用 · …` / `已过期 · …`），pending 不带前缀保持干净。
- **前端**：
  - `apps/workbench/src/api.ts`：新增 `NoteStatus` 类型，`NoteItem.status`。
  - `NotesPage`：卡片元信息行加状态徽标（`.nst.st-pending` 信息蓝 / `.st-used` 成功绿 / `.st-expired` 警示琥珀）；页头新增「全部/待使用/已使用/已过期」状态筛选（`statusFilter` 过滤）；编辑抽屉分类/状态/标签三列并排（g-3），状态 Dropdown 随时改；新建默认 pending。
  - `QuickNoteModal` 不改（创建不传 status，服务端兜底 pending）。
- **样式**：`apps/workbench/src/styles/main.css` 状态徽标三色，沿用 `--info/--ok/--warn` 语义色 + color-mix 浅底。

### 验证与部署

- 服务端单测 127/127（新增 2 例：状态缺省/更新/非法 422 + 时间轴状态前缀）；前端 98/98；双端 typecheck、vite build 通过。
- 另修 `tests/workbench/lib/taxonomy.test.ts` mock 补 status 字段。
- 部署：备份 → DEPLOY.md §3 流程（db push 加列无损）→ 重启探活 → 公网哈希核对 → 灵感页人工核对状态徽标/筛选/抽屉。

### 备注

- 若后续需要「已使用后自动进文章/计划」的联动，可在 status 流转处加钩子，本次不改。
- 状态筛选默认「全部」，避免用户漏看旧灵感；如需默认只显「待使用」可再调。

## 编辑器工具栏大改版 + 随手记三项修复（2026-09-07（二））

### 决策（grill 确认）

- 列表「类型+样式双扩展」：保留 无序/有序/待办，新增 折叠列表（toggleListItem 原生块）+ 无序/有序标记样式切换。
- 嵌入块「在线+本地双支持」：在线视频站识别扩展（B站/YouTube/优酷/腾讯），本地上传视频走原生 video 块播放。
- 超链接 vs 嵌入网页 = 两种需求：行内文字链接（选中加 URL）与整块 iframe 视频卡，保持分开，各自增强。

### 随手记（QuickNoteModal）

- **A1 撤销/丢字 bug**：编辑器新增 `uncontrolled` 非受控模式（value 仅首屏初值、外部值变更不回灌），随手记接入——彻底切断「300ms 防抖上抛 → setText → replaceBlocks 整篇回灌」这条唯一能在编辑中重写内容的路径（表现如撤销）。受控模式（文章编辑页/笔记抽屉）保留原守卫：仅当 value 确与最近上抛不同才回灌。同源排查：文章编辑器/笔记抽屉的受控回灌本就有 lastEmitted 相等短路，正常输入不触发，未发现其余同类路径。
- **A2 高度自适应**：`.nmodal .mded` 去边框、内容底边 = 保存按钮上方分割线（border-bottom + gap 收窄 + min-height 120），无大片空白。
- **A3 标签/分类下拉可新建**：复用 `TagMultiSelect`（Notion 式多选/单选+新建）。标签=多选（全站标签池）；分类=单选（匹配/新建落库 POST /categories），保存时按名解析 id。

### 编辑器核心（EditorToolbar / BlockNoteEditor / customBlocks）

- **B1/B5 图标**：Icon.tsx 扩充 30+ Lucide 风格线性 SVG 编辑图标（undo/redo/B/I/U/S/code/codeblock/quote/列表/缩进/link/image/table/divider/callout/video/eraser/heading/paragraph 等），工具栏全部换 SVG stroke 渲染，消除 emoji/文本图标的锯齿。
- **B2 标题下拉**：正文/标题1-6 下拉（块类型+level 联动）。
- **B3 颜色默认值**：文字色默认=黑、背景色默认=白（正常颜色 swatch），点击即重置。
- **B4 行内代码与代码块并邻**。
- **B6 列表**：折叠列表按钮；无序样式 disc/circle/square、有序 decimal/lower-alpha/upper-alpha/lower-roman/upper-roman 下拉切换。实现：listStyle 自定义 prop（patchListSchemas 注入默认块 schema）→ 非默认渲染为 .bn-block-content 的 data-list-style 属性（CSS 决定 ul/ol 标记）；markdown 真相源用 HTML 注释 `<!-- bn:ul:circle -->` 持久化（lib/listStyles 纯函数 inject/extract + 块级 collect/apply，TDD 18 例）。
- **B7 缩进/减进**：editor.nestBlock/unnestBlock + canNest/canUnnest 禁用态。
- **B8 链接弹层**：输入 URL 确定（弃 window.prompt）；嵌入网页块独立保留。
- **B9 分割线**：原按钮保留并归位（divider 块）。
- **B10 清除格式**：removeStyles 六类标记 + 块还原段落。
- **B11 图片**：弹层二选一——本地上传 / 粘贴图片链接插入。
- **B12/B13 视频**：弹层二选一——本地上传（原生 video 块，<video controls> 播放）/ 在线链接（embed 块，识别 YouTube/B站/优酷/腾讯自动转 iframe，其余渲染链接卡）；服务端 uploads 扩展 video/*（mp4/webm/ogg 魔数嗅探、视频配额默认 2GB/h、raw limit 200MB）。

### 验证与部署

- TDD：lib/listStyles 7 例 + 块级桥 5 例、lib/embedUrl 8 例；uploads 新增视频 2 例。前端 122/122、后端 129/129、双端 typecheck、vite build 通过。
- 部署：备份 `2026-09-07T01-54-26` → DEPLOY.md §3（纯 src/前端改动，db push no-op）→ healthz ok、公网哈希 index-CO2DNSp5.js 与本地一致、/ 与 /notes 均 200。
- 编辑器与随手记人工核对：toolbar 36 按钮 SVG 图标、标题下拉/颜色默认/列表样式弹层、视频按钮在真实环境点击展开。

### 备注

- 列表标记样式的 md run 判定基于当前序列化规则（同种相邻行=同 run）；若 BlockNote 未来改列表导出格式需回归 lib/listStyles 测试。
- 视频上传 express.raw limit 提升到 200MB；超限请求会被 express 拒（413），前端按需提示。
- A1 若在真实环境中仍复现「打字撤销」，下一步用浏览器自动化复现并采集键盘/事务序列。
### 补充：随手记空白修复与撤销复核（2026-09-07（三））

- 用户反馈服务器上随手记三项问题仍在。核实：部署产物确含修复（产线 index-*.js 命中 uncontrolled、BlockNoteEditor chunk 命中 bn:ul 注释桥），非未部署。
- 浏览器实测（本地 dev + 生产同代码）：新 UI 已生效（双行工具栏/折叠列表/视频按钮/标签分类下拉均见）；逐键复测中文输入、空格、退格、方向键均不触发撤销；点击撤销按钮后焦点仍在编辑器（onMouseDown preventDefault），不会连环触发。结论：用户侧大概率运行旧 bundle（未刷新），建议强刷。
- 顺手修复：弹窗内覆写 BlockNote bn-container 最小高 300px→120px、bn-editor 底内边距 64px→4px，内容底边贴合保存按钮分割线（原截图实测 editor 底部与内容区仍有 240px 空白）。
